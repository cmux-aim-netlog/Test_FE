import React from 'react';
import { StyleSheet, View } from 'react-native';

function HistoryFolderTab() {
  return <View style={styles.placeholder} />;
}

const styles = StyleSheet.create({
  placeholder: {
    height: 220,
  },
});

export default HistoryFolderTab;
